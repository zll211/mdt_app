import 'package:flutter/material.dart';

const primaryColor = Color(0xFF609EFE);
const consultationAdd = Color(0xFF67C23A);
const consultationEdit = Color(0xFF609EFE);
const consultationFailed = Color(0xFFF56C6C);
const consultationReport = Color(0xFFE6A23C);

const titleColor = Color(0xFF202020);
const textColor = Color(0xFF828996);

const normalPadding = 16.0;

const borderColor = Color(0xFFEAEAEA);

const appBarShadow = [BoxShadow(offset: Offset(0, 1), color: Color(0x33609EFE), blurRadius: 7.0, spreadRadius: 2.0)];

const normalShadow = [BoxShadow(blurRadius: 6.0, color: Color(0x0F000000))];

